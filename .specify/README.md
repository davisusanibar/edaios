# Spec Kit en EDAIOS

Spec Kit orquesta ocho fases; EDAIOS decide mediante Constitución, ADR y gates:
constitution → specify → clarify → checklist → plan → tasks → analyze → implement.

Los comandos en `.specify/commands/` son fuente. Claude, Codex, Copilot y el
preset Core son derivados regenerables.
